void main() {
  // const cor = 'vermelho';
  var cor = Cores.vermelho;

  if (cor == Cores.vermelho) {}
}

// Interessante para a crioação de valores isomples e estáticos
// Facilitando até a criação de switch e afins
enum Cores { azul, vermelho, laranja, verde, preto }
